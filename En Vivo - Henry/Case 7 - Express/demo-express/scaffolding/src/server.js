const express = require("express");
//* Requerimos express y su función que nos da todas las funcionalidades

const server = express();
//* La función que se requiere de express hace falta ser llamada para que nos de todos los métodos de express y ya este totalmente disponible.

module.exports = server;
