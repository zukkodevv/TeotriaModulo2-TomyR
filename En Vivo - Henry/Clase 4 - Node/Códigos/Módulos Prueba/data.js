const user = "damian";
const a = 7;
const b = 10;

//* Valores que definimos para después exportar y usar en otros módulos

module.exports = {
  user,
  a,
  b,
};

//* Le indicamos al código que exporte solo los valores brindados
