//* Acá va a estar definida la lógica de la petición a la ruta

const testController = (req, res) => {
    //* Definimos la función testController que recibe dos parametros, request de 
};

module.exports = { testController };
